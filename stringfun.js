let person_name = "afra";
console.log("lower case: " + person_name.toLowerCase()); // convert name to lower case
console.log("UPPER CASE: " + person_name.toUpperCase()); // convert name to upper case
export {};
