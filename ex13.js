let trans_mode = ["Scooty", "Bicycle", "Jet ski", "Car"]; // array declaration and initialization
for (let i = 0; i < trans_mode.length; i++) {
    console.log("I would like to own a " + trans_mode[i]); //printing array using loop
}
export {};
